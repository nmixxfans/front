'use client'

import styled from "styled-components";

const Container = styled.div`
  display: flex;
`

interface ContainerProps {
}

export function EctContainer({ }: ContainerProps) {
  return (
    <Container>
      {/* <Debut /> */}
    </Container>
  )
}